[build]
command = "export NODE_OPTIONS=--openssl-legacy-provider && npm install --legacy-peer-deps && npm run build"
publish = "build"

[build.environment]
NODE_VERSION = "16"
NPM_FLAGS = "--legacy-peer-deps"
NODE_OPTIONS = "--openssl-legacy-provider" 